# Frontend runtime configuration (required for live chain + matchmaking)
# Preferred contract env var:
NEXT_PUBLIC_PIXEL_ROYALE_ADDRESS=
# Backward-compatible alias (either value works):
NEXT_PUBLIC_GAME_CONTRACT_ADDRESS=
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001
